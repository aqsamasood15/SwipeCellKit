# Uncomment this line to define a global platform for your project
platform :ios, '9.0'

inhibit_all_warnings!
use_frameworks!

  target :'SwipeCellKit' do
    pod 'DataSource', :git => 'https://github.com/Fueled/DataSource.git'
  end
